#pragma once

namespace engine {

// Fase 0: cadena de humo del pipeline de build (PLAN.md §7.1).
// El registry, IModel/IProduct/IMeasure y el bootstrap llegan en Fase 1-2 (PLAN.md §5.4).
double ping();

} // namespace engine
